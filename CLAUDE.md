# Project notes — Ritwiz Sharma portfolio

- Always keep photos/imagery full-color — never grayscale or tint them. Monochrome/B&W directions apply to UI (text, blocks, borders) only.
- Sep 13: `Shenanigans.dc.html` renamed to `OffHours.dc.html`; nav label and page title are now "Off Hours" everywhere. Data file is still `shenanigans-sets.js`. Deploy slug should become `/off-hours` (with a 301 from `/shenanigans`) on the next build.
- Aug 18: `Shenanigans.dc.html` rebuilt on the dark Home language (direction C "Reel") — six horizontal filmstrip bands with auto-drift + drag scrub; set data lives in `shenanigans-sets.js`, which must ship with any export. The old light/cream parallax-plate version and the A/B options were dropped.
- Aug 18: deleted the last exploration pages (`About Options`, `Home Dossier`, `Home Variations`, `Hover Studies`, `Intro Studies`) — nothing linked to them. Hover tags (`data-cap`) stripped from all project pages; project sections use sequential `Images N` labels everywhere.
- Live page set (July 29): `Home.dc.html`, `About.dc.html`, `Shenanigans.dc.html` + six `*-minimal.dc.html` project pages. Everything else was deleted July 29 — the `*-wix.dc.html` import copies, the older non-minimal project pages, `Home-exp`, `Hover Labels Audit`. Wix is no longer a deploy target; hosting is Netlify from `export/netlify/`.
- Aug 3: every Wix CDN image was pulled local — `aoc/` (34), `vita/` (28), `altr/` (+5), `home/thumb-*.jpg` (6 hover thumbs, 800px). Stills are 1600px max, JPEG q84; the 5 GIFs are raw Wix exports and still heavy (`aoc/play-dustbin-in-motion.gif` 11 MB, `vita/luggage-animation-1..4.gif` 4.5–12 MB) — re-export or convert to MP4 when possible. No page loads `static.wixstatic.com` any more.
- Sep 3: all nine GIFs replaced with WebM `<video autoplay loop muted playsinline preload="metadata">` — `vita/luggage-animation-1..4.webm`, `ergoplay/transform.webm`, `ergoplay/gaming-shell-360.webm`, `aoc/play-dustbin-in-motion.webm`, `uploads/Packaging.webm`, `uploads/VID-20251118-WA0004.webm` (Shenanigans template got an `im.isVid` branch; preload skips video). The original `.gif` files are still on disk as unreferenced backup — don't copy them into deploy builds.
- Sep 12: the six `*-minimal.dc.html` project pages were flipped to dark to match Home/About/Shenanigans — `--pg-bg:#0B0B0B`, `--pg-ink:#F4F2EE`, hairlines/mutes on `rgba(244,242,238,…)`; inverted hover fills, boot screen, progress bar and cursor tag too. Per-project accent hexes (#17B26A, #7A3BFF, #2E6BFF, #FF7A00, #F4C21A, #E4432C) unchanged. The whole site is now dark on every page.
- Only edit the `.dc.html` sources by default; refresh `export/netlify/` only when the user asks for a deploy build.
- Aug 18 deploy build: `export/netlify/` regenerated from the nine live `.dc.html` sources (link map: Home→index, About→about, Shenanigans→shenanigans, *-minimal→snapin/aakaar/ergoplay/vita/altr/art-of-concealing). 326 asset files — pruned to only what the pages reference (was 938). `_headers` updated (home/* → nda/*), `_redirects` added for old `.dc.html` URLs. Rebuild recipe: rewrite `.dc.html` links, copy `support.js`, copy referenced files under snapin/web/uploads/altr/aoc/vita/ergoplay/nda.
- Aug 4 design-system normalization: site is DARK-ONLY (`data-rwtheme` hardcoded to 'dark', theme toggles removed), single fixed accent #2E6BFF (bright blue; accent picker removed), all pages on Overused Grotesk (Archivo fully gone), Home-style segmented header (.rwTrack/.rwSeg) on every page, rounded-rect chips (.66em) instead of 9999px pills, headings weight 700 (Home list rows 700/hover 800), hairlines rgba(237,233,228,.16). Emojis live ONLY in the marquees (Home, Home v2, About) — nowhere else. Confidential credits on Home are company-name links + year + generic descriptor only.
- Home.dc.html current state: colorful intro word-cloud → list view, no dashed borders, color hover images.
- The single-file build (`Portfolio.dc.html` + `export/Portfolio-standalone.html`) was deleted July 26 — the site stays multi-file. `web/` still holds the compressed WebP copies (≤1600px) of `snapin/` and `uploads/opt/` if a lightweight build is ever wanted again.
